# Ambient beds (royalty-free)

Continuous under-narration beds for historian (`score_mode=ambient_soft`, −32 dB)
and napstorian fallback when trailer pack is empty.

Add `.wav` files here. Optional licensed providers (Epidemic / Artlist) are
configured later via `competitor_style_profiles.json` → `audio.epidemic_playlist_id`
/ `artlist_playlist_id` — not required for P0.
