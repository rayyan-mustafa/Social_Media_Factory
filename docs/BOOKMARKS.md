# Production models

This document moved to [PRODUCTION_MODELS.md](PRODUCTION_MODELS.md).

The nine business lines are production services (not bookmarks). Each has its
own worker and, except YouTube_Shorts, a MinIO folder under `production/{Model}/`.
